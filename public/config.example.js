// Copy this file to config.js and replace the values with your Entra app registration details.
// config.js is git-ignored so your real client/tenant IDs stay local.
window.__APP_CONFIG__ = {
  clientId: "REPLACE_WITH_ENTRA_APP_CLIENT_ID",
  tenantId: "common",
  redirectUri: "http://localhost:3000",
};
